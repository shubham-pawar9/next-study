import { CSSProperties } from "react";

export const navbarStyle: Record<string, CSSProperties> = {
    root: {
        margin: '8px 12px',
    },
    linkSection: {
        padding: '8px',
        background: '#e279ac',
        borderRadius: '4px',
    }
}